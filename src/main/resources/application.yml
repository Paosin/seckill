spring:
    mvc:
      view:
        prefix: "/"
        suffix: ".html"
    profiles:
      active: dev
mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml